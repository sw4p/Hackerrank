n = int(input())
arr = [int(x) for x in input().split()]
if arr[0] >= arr[1]:
	max1 = arr[0]
	max2 = arr[1]
else:
	max1 = arr[1]
	max2 = arr[0]
	
for i in arr:
	if i > max1:
		max2 = max1
		max1 = i
	elif i < max1 and i > max2:
		max2 = i

print(max2)
